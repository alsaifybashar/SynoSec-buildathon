services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "${POSTGRES_PORT:-55432}:5432"
    environment:
      POSTGRES_DB: synosec
      POSTGRES_USER: synosec
      POSTGRES_PASSWORD: synosec
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U synosec -d synosec"]
      interval: 2s
      timeout: 5s
      retries: 30
    volumes:
      - postgres_data:/var/lib/postgresql/data

  # ── Intentionally vulnerable target (safe demo only) ─────────────────
  vulnerable-target:
    build:
      context: ./demos/vulnerable-app
    container_name: synosec-target
    hostname: synosec-target
    ports:
      - "8888:8888"
    environment:
      PORT: 8888

  ollama:
    image: ollama/ollama:latest
    profiles:
      - local-llm
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    healthcheck:
      test: ["CMD", "ollama", "list"]
      interval: 10s
      timeout: 5s
      retries: 12

  ollama-init:
    image: ollama/ollama:latest
    profiles:
      - local-llm
    depends_on:
      ollama:
        condition: service_healthy
    environment:
      OLLAMA_HOST: http://ollama:11434
      OLLAMA_MODEL: ${LLM_LOCAL_MODEL:-qwen3:1.7b}
    entrypoint:
      - /bin/sh
      - -c
      - ollama pull "$${OLLAMA_MODEL}"
    restart: "no"

  backend:
    build:
      context: .
      dockerfile: apps/backend/Dockerfile
    ports:
      - "${BACKEND_PORT:-3001}:${BACKEND_PORT:-3001}"
    volumes:
      - .:/workspace
      - backend_node_modules:/workspace/apps/backend/node_modules
      - contracts_node_modules:/workspace/packages/contracts/node_modules
      - root_node_modules:/workspace/node_modules
    working_dir: /workspace
    depends_on:
      postgres:
        condition: service_started
      vulnerable-target:
        condition: service_started
    environment:
      BACKEND_PORT: ${BACKEND_PORT:-3001}
      DATABASE_URL: postgres://synosec:synosec@postgres:5432/synosec
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY}
      CLAUDE_MODEL: ${CLAUDE_MODEL:-claude-sonnet-4-6}
      LLM_PROVIDER: ${LLM_PROVIDER:-anthropic}
      LLM_LOCAL_BASE_URL: http://ollama:11434
      LLM_LOCAL_API_PATH: ${LLM_LOCAL_API_PATH:-/api/generate}
      LLM_LOCAL_MODEL: ${LLM_LOCAL_MODEL:-qwen3:1.7b}
      SCAN_MAX_DEPTH: ${SCAN_MAX_DEPTH:-4}
      SCAN_MAX_DURATION_MINUTES: ${SCAN_MAX_DURATION_MINUTES:-20}
      SCAN_RATE_LIMIT_RPS: ${SCAN_RATE_LIMIT_RPS:-5}
      AGENT_MAX_ITERATIONS: ${AGENT_MAX_ITERATIONS:-5}
      TOOL_EXECUTION_MODE: ${TOOL_EXECUTION_MODE:-connector}
      CONNECTOR_SHARED_TOKEN: ${CONNECTOR_SHARED_TOKEN:-synosec-connector-dev}
      FRONTEND_URL: ${FRONTEND_URL:-http://localhost:5173}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    command: sh -c "pnpm install --frozen-lockfile || pnpm install && pnpm --filter @synosec/contracts build && pnpm --filter @synosec/backend prisma:generate && pnpm --filter @synosec/backend prisma:push && pnpm --filter @synosec/backend dev"

  connector:
    build:
      context: .
      dockerfile: apps/connector/Dockerfile
    depends_on:
      - backend
    environment:
      CONNECTOR_CONTROL_PLANE_URL: http://backend:${BACKEND_PORT:-3001}
      CONNECTOR_SHARED_TOKEN: ${CONNECTOR_SHARED_TOKEN:-synosec-connector-dev}
      CONNECTOR_NAME: ${CONNECTOR_NAME:-local-dev-connector}
      CONNECTOR_RUN_MODE: ${CONNECTOR_RUN_MODE:-dry-run}
      CONNECTOR_ALLOWED_CAPABILITIES: ${CONNECTOR_ALLOWED_CAPABILITIES:-passive,web-recon,network-recon,content-discovery,active-recon}
      CONNECTOR_ALLOWED_SANDBOX_PROFILES: ${CONNECTOR_ALLOWED_SANDBOX_PROFILES:-network-recon,read-only-parser,active-recon}
      CONNECTOR_ALLOWED_PRIVILEGE_PROFILES: ${CONNECTOR_ALLOWED_PRIVILEGE_PROFILES:-read-only-network,active-network}
    volumes:
      - .:/workspace
      - connector_node_modules:/workspace/apps/connector/node_modules
      - contracts_node_modules:/workspace/packages/contracts/node_modules
      - root_node_modules:/workspace/node_modules
    working_dir: /workspace

  frontend:
    build:
      context: .
      dockerfile: apps/frontend/Dockerfile
    environment:
      VITE_API_PROXY_TARGET: http://backend:${BACKEND_PORT:-3001}
      VITE_DEV_PORT: ${VITE_DEV_PORT:-5173}
    ports:
      - "${VITE_DEV_PORT:-5173}:${VITE_DEV_PORT:-5173}"
    depends_on:
      - backend
    volumes:
      - .:/workspace
      - frontend_node_modules:/workspace/apps/frontend/node_modules
      - contracts_node_modules:/workspace/packages/contracts/node_modules
      - root_node_modules:/workspace/node_modules
    working_dir: /workspace
    command: sh -c "pnpm install --frozen-lockfile || pnpm install && pnpm --filter @synosec/contracts build && pnpm --filter @synosec/frontend dev -- --host 0.0.0.0 --port ${VITE_DEV_PORT:-5173}"

volumes:
  postgres_data:
  ollama_data:
  backend_node_modules:
  contracts_node_modules:
  frontend_node_modules:
  connector_node_modules:
  root_node_modules:
